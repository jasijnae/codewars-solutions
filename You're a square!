var isSquare = function(n){
  
  n = Math.sqrt(n) // find the square root of n
  return Number.isInteger(n) // return T/F if the square root is a number 
  
}


/*
input - will be passed one number
output- need to return true or false
task- determin if number passed can be squared
methods for determining square root
  -Math.sqrt(number) to return the squr of a number
  -Number.isInteger() to determine if the that num is 
    a whole num
  - a conditional to determine if this or that
      return Number.isInteger() for if T or F
*/
